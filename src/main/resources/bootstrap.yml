server:
  port: 8142

spring:
  application:
    name: zCloud2ConfigClient
  cloud:
    config:
      uri: http://localhost:8340
      profile: default
      label: master

#JCE对称加密
encrypt:
  key: wenyixicodedog